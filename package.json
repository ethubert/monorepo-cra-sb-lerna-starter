{
  "name": "365",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "publishConfig": {
    "registry": "http://0.0.0.0:4873/"
  },
  "scripts": {
    "publish": "lerna publish --no-git-tag-version --no-push --registry=\"http://localhost:4873/\""
  },
  "version": "0.1.11"
}
